version: "3.9"
services:
  pg:
    image: postgres:latest
    env_file:
      - config/pg.env
    ports:
      - "5432:5432"

  tgbot:
    volumes:
      - ./config:/config
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    restart: unless-stopped
    depends_on:
      - pg
    links:
      - pg
